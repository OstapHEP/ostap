#!/usr/bin/env bash
LCG=$1
CMTCONFIG=$2

mkdir build
cd    build

## OLD 
## source /cvmfs/sft.cern.ch/lcg/views/${LCG}/${CMTCONFIG}/setup.sh
## cmake .. -DCMAKE_INSTALL_PREFIX=./INSTALL/ -GNinja && ninja && ninja install

source /cvmfs/sft.cern.ch/lcg/views/${LCG}/${CMTCONFIG}/setup.sh
source ../.aux/.build-ostap ../ ./INSTALL 

