# v3.7.5.0

  1. make the environment script `thisostap.sh` script "universal": not it should be OK for `sh/bash/ksh/zsh"
  1. the first version of the environment `thisostap.csh` script to setup the environment for `csh/tcsh`.
  1. all scripts from `scripts` directory now install into `bin`  directory: for better coherency and simlification of conda builds
   
## Bug fixes

   1. Bug fixes for compilation for OSX
   

