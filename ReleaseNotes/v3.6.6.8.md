# v3.8.6.8

## New features

  1. replace Wasserstein's distance  with Standartized Wasserstein's distance 
  1. add `prescale` & `prescale2` arguments to `data_compare`
  1. add update/tune default hypoerparameters for adversarial validation tools and reweighters 
  1. add new density-reweighter based on PyTorch. Currently it seems to be the best one...

## Bug fixes

  1. fix an important bug in `Adversatial Validation`
  
## Backward incompatible
